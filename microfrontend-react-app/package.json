{
  "private": true,
  "scripts": {
    "start": "lerna run --scope @microfrontend-react-app/* --parallel start",
    "build": "lerna run --scope @microfrontend-react-app/* build",
    "serve": "lerna run --scope @microfrontend-react-app/* --parallel serve",
    "clean": "lerna run --scope @microfrontend-react-app/* --parallel clean"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
