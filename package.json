{
  "name": "root",
  "private": true,
  "workspaces": [
    "microfrontend-react-app/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --ignore-scripts",
    "build": "lerna run --parallel build"
  }
}
